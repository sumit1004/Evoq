# Match Management

Matches are independent children of groups and use `SCHEDULED → LIVE → COMPLETED`. Match number uniqueness is database-enforced per group. Match results reference stable `matchId` values and cannot overwrite another match's records.
