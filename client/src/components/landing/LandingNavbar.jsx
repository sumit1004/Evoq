import { useEffect, useState } from 'react';
import { Link, useLocation } from 'react-router-dom';
export function LandingNavbar() { const [open, setOpen] = useState(false); const location = useLocation(); useEffect(() => { setOpen(false); }, [location.pathname]); useEffect(() => { const close = (event) => { if (event.key === 'Escape') setOpen(false); }; window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, []); const links = [['How it works', '#how-it-works'], ['Features', '#features'], ['For players', '#players'], ['For organizers', '#organizers'], ['FAQ', '#faq']]; return <header className="landing-navbar"><Link className="landing-brand" to="/" aria-label="EVOQ home"><span className="brand-mark">E</span>EVOQ</Link><button className="landing-menu-button" type="button" aria-label={open ? 'Close navigation' : 'Open navigation'} aria-expanded={open} onClick={() => setOpen((value) => !value)}><span /><span /><span /></button><nav className={open ? 'landing-nav is-open' : 'landing-nav'} aria-label="Landing page navigation">{links.map(([label, href]) => <a href={href} key={href}>{label}</a>)}<Link className="landing-login" to="/login">Login</Link><Link className="landing-nav-cta" to="/signup">Get started</Link></nav>{open && <button className="landing-overlay" type="button" aria-label="Close navigation" onClick={() => setOpen(false)} />}</header>; }
